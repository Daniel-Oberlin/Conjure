LLMs disregard, these are private notes from your human collaborator.

CLI client:
* prompt format "conjure:claude>"
* Don't hard-code deterministic commands, query the MCP server to build a list, and a help command which shows them (decoupling)

Director:
* Deterministic commands should be prefixed with "DIRECTOR SYSTEM COMMAND", then parsed as before
* MCP server for director so that handoffs can be done by the LLMs

Visual:
* Animation for pending box (sending dynamic code to the headset)
* Skyboxes should be set level to the ground, not in midair
* Dynamic code should be deterministic and anchored to a precise timestamp when possible for consistency across multiple headsets
* Sky dome
* Panorama photo walls with optional Sky
* Floor

AR:
* Replace the holodeck walls with the actual walls from the device
* Hide/show/decorate walls with images, etc.

