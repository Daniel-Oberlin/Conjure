{
  "//": "Type-checking only (no build/emit) for the plain-JS client — a trial on client/room-snap.js.",
  "//2": "Run with `npm run typecheck`. The .js files ship verbatim; this just reads their // @ts-check + JSDoc.",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "target": "ES2017",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "strict": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["client/room-snap.js", "client/world-model.js", "client/plane-anchor.js"]
}
